package com.rekj.core.web.query.rule.impl;

import com.rekj.core.web.query.entity.JudgeScript;
import com.rekj.core.web.query.rule.ISqlScript;

public class SqlScript implements ISqlScript {
	public String getSQL(JudgeScript judgeScript) {
		if (judgeScript == null) {
			return "";
		}
		return judgeScript.getValue();
	}
}
